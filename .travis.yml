language: java
jdk: oraclejdk8

addons:
  sonarqube:
    organization: "ubu.gii" # the key of the org you chose at step #3
    #token:
    #  secure:960f6ae93d10e343ac0e2963c99dfaff0b5d1216 # encrypted value of your token
script:
  # other script steps might be done before running the actual SonarQube analysis
  - sonar-scanner